# Restore historical content without rewinding main

Historical cloud versions remain immutable and read-only. Restoring copies the selected content into a reviewable working copy that may be explicitly saved as the next main version, while branching from history remains available for experimentation; main never moves backwards and no intervening history is deleted.
