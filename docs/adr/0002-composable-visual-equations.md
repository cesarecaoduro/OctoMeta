# Use visual equations with composable live references

An equation is one visually edited mathematical expression that may contain authored notation and multiple live workbook references. This replaces mutually exclusive static and single-bound modes, making common authoring direct while retaining an optional raw-TeX escape hatch for advanced notation; it requires the stored equation format to preserve reference identity independently of rendered text.
