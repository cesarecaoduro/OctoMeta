# Support offline owner workspaces without automatic cloud sync

Owners may create and edit local documents, branches, and previously downloaded working copies offline with normal local durability and undo. Cloud-only content, version creation, reconciliation, sharing, and history access require connectivity, and reconnection never publishes automatically; this preserves explicit cloud-save intent while providing useful offline authoring.
