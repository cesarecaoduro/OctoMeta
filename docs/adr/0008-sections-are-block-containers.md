# Model sections as block containers

A section has stable identity and owns one ordered level of child blocks rather than acting as a decorative heading. This enables notebook-like boundaries, collapse, group movement, deletion, and complete undo at the cost of introducing a deliberate two-level document hierarchy; deeper arbitrary nesting is not implied.
