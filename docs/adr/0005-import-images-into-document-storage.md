# Import images instead of linking remote files

Image blocks own imported bytes from file selection, drag-and-drop, or clipboard paste, together with their caption, alternative text, display size, and alignment. New bytes are stored in the browser workspace and uploaded only when a cloud version referencing them is explicitly saved; remote hotlinks are excluded initially so document content remains reproducible and does not depend on third-party availability.
