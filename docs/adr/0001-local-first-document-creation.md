# Create documents locally before their first cloud save

New documents exist only in the browser workspace until the user explicitly saves them. This prevents ordinary creation and editing from producing cloud document, graph, chip, block, workbook, asset, or undo records; the trade-off is that a local document remains device-bound unless the user saves it as cloud version 1 or exports a portable copy.
