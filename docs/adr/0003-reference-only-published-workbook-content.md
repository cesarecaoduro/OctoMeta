# Reference only explicitly published workbook content

Document references may target only workbook content that the author explicitly publishes under a meaningful name. The initial model supports scalar published values; a later extension may publish selected ranges as live tables. This excludes arbitrary non-empty cells from reference pickers, trading a small publication step for stable intent and a manageable authoring vocabulary.
